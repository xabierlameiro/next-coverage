## What

## Why

## Checklist

- [ ] The title is a Conventional Commit (`fix: …`, `feat: …`); it becomes the commit on `main`
- [ ] Tests cover the new behaviour
- [ ] `pnpm typecheck && pnpm lint && pnpm test` pass locally
- [ ] A new condition carries the evidence it cites, and the reason it stays silent when it does
