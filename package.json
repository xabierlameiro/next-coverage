{
  "name": "next-coverage",
  "version": "0.1.0",
  "description": "API surface coverage report for Next.js App Router projects: what you use, what would apply, what does not.",
  "keywords": [
    "nextjs",
    "app-router",
    "coverage",
    "api-surface",
    "audit",
    "cli"
  ],
  "license": "MIT",
  "author": "Xabier Lameiro",
  "homepage": "https://github.com/xabierlameiro/next-coverage#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/xabierlameiro/next-coverage.git"
  },
  "bugs": {
    "url": "https://github.com/xabierlameiro/next-coverage/issues"
  },
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "packageManager": "pnpm@10.33.0",
  "bin": {
    "next-coverage": "dist/cli.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "fixtures": "node test-support/materialize.mjs",
    "pack:smoke": "node scripts/pack-smoke.mjs",
    "prepublishOnly": "npm run typecheck && npm run test && npm run build",
    "prepare": "husky >/dev/null 2>&1 || true"
  },
  "dependencies": {
    "tinyglobby": "^0.2.15",
    "typescript": "^6.0.3",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.13",
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2",
    "@types/node": "^26.5.1",
    "husky": "^9.1.7",
    "tsup": "^8.5.0",
    "vitest": "^5.0.0"
  },
  "pnpm": {
    "overrides": {
      "esbuild": "^0.28.1"
    }
  },
  "sideEffects": false
}
