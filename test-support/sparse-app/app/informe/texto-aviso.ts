// Exists so `Aviso.tsx` reaches one module nothing else reaches: the exclusive reach the client
// directive's strict condition states beside the file, as a count of modules and never as bytes.
export const TEXTO_AVISO = 'Cifras provisionales';
