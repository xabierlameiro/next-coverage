import type { NextConfig } from 'next';

// Deliberately bare. incomplete-app turns flags on to reach flag-gated entries; this project
// exists for conditions that read structure, and a flag here would only add ways to differ.
const nextConfig: NextConfig = {};

export default nextConfig;
