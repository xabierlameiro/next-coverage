// Case: `authInterrupts`, second convention. Present and inert for the same reason, so the
// condition has to report both files rather than the first one it finds.
export default function Unauthorized() {
  return <p>Inicia sesion para continuar.</p>;
}
